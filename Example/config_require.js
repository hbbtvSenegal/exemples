var require = { 
                baseUrl: './',  // chemin vers le dossier principale de l'application (ici dossier contenant index.js), sert de référence pour les chemins relatifs dans paths 
                paths: { 
                        exemple : './', //aliase de l'application (le fichier in 
                        antie : 'antie/static/script' //aliase du dossier antie/static/script 
                }, 
                priority: [], 

                callback: function() {} 
        }
